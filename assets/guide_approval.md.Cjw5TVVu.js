import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Asking a person","description":"","frontmatter":{},"headers":[],"relativePath":"guide/approval.md","filePath":"guide/approval.md","lastUpdated":1788284050000}`),a={name:`guide/approval.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="asking-a-person" tabindex="-1">Asking a person <a class="header-anchor" href="#asking-a-person" aria-label="Permalink to “Asking a person”">​</a></h1><p>Twenty-five of the 62 tools remove content, move it, or change who may reach the wiki. All twenty-five <strong>ask a person first</strong>.</p><p>Not a <code>confirm: true</code> argument the model can set. Not a token the model reads out of its own previous result. A dialog, raised through <a href="https://modelcontextprotocol.io/specification/2025-11-25/client/elicitation" target="_blank" rel="noreferrer">MCP elicitation</a>, that goes to the client and is shown to whoever is sitting there.</p><p>The specification says a client <em>should</em> keep a human in the loop:</p><blockquote><p>there <strong>SHOULD</strong> always be a human in the loop with the ability to deny tool invocations</p></blockquote><p>This server does not rely on that. It raises the question itself, and until an answer comes back, nothing happens.</p><h2 id="what-asks-and-when" tabindex="-1">What asks, and when <a class="header-anchor" href="#what-asks-and-when" aria-label="Permalink to “What asks, and when”">​</a></h2><table tabindex="0"><thead><tr><th>Group</th><th>Tools</th></tr></thead><tbody><tr><td>Pages</td><td><code>move_page</code> · <code>delete_page</code> · <code>convert_page_editor</code> · <code>restore_page_version</code> · <code>purge_page_history</code> · <code>migrate_pages_locale</code></td></tr><tr><td>Tags</td><td><code>update_tag</code> · <code>delete_tag</code></td></tr><tr><td>Assets</td><td><code>rename_asset</code> · <code>delete_asset</code></td></tr><tr><td>Comments</td><td><code>update_comment</code> · <code>delete_comment</code></td></tr><tr><td>Users</td><td><code>create_user</code> · <code>update_user</code> · <code>delete_user</code> · <code>set_user_active</code> · <code>verify_user</code> · <code>set_user_tfa</code> · <code>reset_user_password</code></td></tr><tr><td>Groups</td><td><code>update_group</code> · <code>delete_group</code> · <code>assign_user_to_group</code> · <code>unassign_user_from_group</code></td></tr><tr><td>API keys</td><td><code>revoke_api_key</code> · <code>set_api_state</code></td></tr><tr><td>everything else</td><td>never asks</td></tr></tbody></table><p><code>update_page</code> is deliberately <strong>not</strong> on that list, and it is the clearest example of why a per-tool judgement beats a rule about verbs: Wiki.js keeps page history, so an edit can be read back and restored. <code>update_comment</code> <em>is</em> on it, because comments have no history. Same verb, different backend, different answer.</p><p>Three maintenance tools came <strong>off</strong> the list: <code>flush_page_cache</code>, <code>rebuild_page_tree</code> and <code>rebuild_search_index</code>. They are instance-wide and slow, and that was the argument for gating them — but nothing is lost by any of them. The cost is time, not content, and a dialog in front of an operation that loses nothing is how people learn to tick without reading, which spends exactly the attention <code>purge_page_history</code> needs. Their <code>confirm_token</code> parameter went with the guard, so a caller that still sends one is told rather than quietly ignored.</p><h2 id="what-the-dialog-contains" tabindex="-1">What the dialog contains <a class="header-anchor" href="#what-the-dialog-contains" aria-label="Permalink to “What the dialog contains”">​</a></h2><p>Ids, paths and server-side values. Never a page title, description, comment or body.</p><p>A wiki is precisely a place where text is stored so that it can be read later, which makes every page a channel for whoever can edit it — and the prompt is read by a model at the exact moment it is deciding. A path is checked to be a bare identifier, with no whitespace, quotes or control characters, before it is interpolated at all.</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>This will migrate every page from locale &quot;de&quot; to locale &quot;en&quot;.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Pages that already exist in the target locale are skipped, and the operation</span></span>
<span class="line"><span>cannot be reversed by running it the other way round.</span></span></code></pre></div><p>The approval is bound to its target, so one obtained for a call cannot be replayed against another. For a <em>set</em> of targets the binding is a fingerprint of the exact list: an approval for <code>[&quot;a&quot;]</code> does not execute <code>[&quot;a&quot;, &quot;b&quot;]</code>.</p><h2 id="clients-that-cannot-show-a-dialog" tabindex="-1">Clients that cannot show a dialog <a class="header-anchor" href="#clients-that-cannot-show-a-dialog" aria-label="Permalink to “Clients that cannot show a dialog”">​</a></h2><p>Not every MCP client implements elicitation, and a stateless gateway may not be able to speak for the one it is currently serving. Rather than refuse to work — which pushes people towards switching the guard off entirely — the tool falls back to a <strong>two-call token</strong>: the first call returns a random string, the second has to quote it back.</p><p>Be clear about what that proves, because this server is:</p><blockquote><p>the token proves the call was made twice with the same arguments, and nothing more.</p></blockquote><p>A model can read the token out of the first result and call again in the same turn without anybody seeing it. It catches a widened target set; it does not catch a model that was talked into the whole thing. The fallback text says so rather than implying somebody approved.</p><h2 id="switching-the-dialog-off" tabindex="-1">Switching the dialog off <a class="header-anchor" href="#switching-the-dialog-off" aria-label="Permalink to “Switching the dialog off”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">ELICITATION</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">false</span></span></code></pre></div><p>Default is <code>true</code>. <code>false</code> does <strong>not</strong> remove the guard — it takes the fallback path above, which means the token. There is no setting in which a guarded call goes unannounced.</p><p>Use it where a dialog is the wrong shape rather than an unwanted one: a scheduled job, a test harness, a client whose dialog interrupts something else.</p><div class="warning custom-block"><p class="custom-block-title">It is deliberately not prefixed</p><p><code>ELICITATION</code> has no <code>WIKIJS_</code> in front of it, so one <code>export ELICITATION=false</code> — or one <code>-e ELICITATION=false</code> in a compose file — reaches <strong>every</strong> MCP server in that environment, not just this one. That is the point of it and also its risk.</p><p>Two things make it visible rather than silent:</p><ul><li><p>a server started with it off prints one line at startup, in the log of every server it actually reached:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>wikijs-mcp: ELICITATION=false — guarded tools fall back to the two-call token</span></span></code></pre></div></li><li><p>the fallback text names the server that did not ask, instead of blaming a client that was working fine. :::</p></li></ul><p>Anything other than <code>true</code> or <code>false</code> — <code>1</code>, <code>off</code>, <code>yes</code> — <strong>stops the server</strong> with exit code 1 and a message naming both valid values. This is the only variable in this family that defaults to <em>on</em>: a typo that fell back to the default would leave the dialog running while the operator believed it was off, and there would be nothing to tell them.</p><h2 id="annotations-are-the-other-half-and-they-are-only-a-hint" tabindex="-1">Annotations are the other half, and they are only a hint <a class="header-anchor" href="#annotations-are-the-other-half-and-they-are-only-a-hint" aria-label="Permalink to “Annotations are the other half, and they are only a hint”">​</a></h2><p>Every tool of this server declares all four MCP tool annotations — <code>readOnlyHint</code>, <code>destructiveHint</code>, <code>idempotentHint</code>, <code>openWorldHint</code> — so a client can tell before it calls what a call would do. See <a href="/reference/tools">Tools</a>.</p><p>They are advice, and the specification says so:</p><blockquote><p>clients <strong>MUST</strong> consider tool annotations to be untrusted unless they come from trusted servers</p></blockquote><p>An annotation is something a client may ignore. The dialog is not: it is enforced here, on the server side, and no answer means no change. The two are different claims — the annotation says what a call <em>does</em>, the dialog decides whether it <em>happens</em> — which is why a tool can be marked destructive without being guarded. The binding covers everything that decides <em>what</em> is touched, each value labelled by its role — so an approval for “delete user 1, reassign to 2” will not run “delete user 2, reassign to 1”.</p><h2 id="behind-a-gateway" tabindex="-1">Behind a gateway <a class="header-anchor" href="#behind-a-gateway" aria-label="Permalink to “Behind a gateway”">​</a></h2><p>Both protocol revisions are handled from one code path. On <code>2025-11-25</code> the question is pushed to the client; on <code>2026-07-28</code> there is no server→client channel at all, so the call returns <code>input_required</code>, ends, and the client retries carrying the answer.</p><p>That answer arrives as ordinary request content, which the SDK does not validate — so the state that ties an answer to its question is sealed (HMAC). A reply whose seal does not open, or opens onto a different target, counts as <strong>no answer</strong> and produces a fresh question rather than an error. The likeliest cause is not an attack: it is a gateway that put the server to sleep while the person was reading.</p><p>If you run this behind <a href="https://github.com/ni-c/mcp-hub" target="_blank" rel="noreferrer">mcp-hub</a>, the hub passes elicitation through in both directions; see its <a href="https://ni-c.github.io/mcp-hub/guide/elicitation" target="_blank" rel="noreferrer">elicitation guide</a>.</p></div>`,25)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};