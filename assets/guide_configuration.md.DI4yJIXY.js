import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1788345439000}`),a={name:`guide/configuration.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h1><p>See the <a href="/reference/environment">environment variable reference</a> for the full table.</p><h2 id="getting-a-token" tabindex="-1">Getting a token <a class="header-anchor" href="#getting-a-token" aria-label="Permalink to “Getting a token”">​</a></h2><p>Administration → API → <strong>API Access</strong>. Switch the API on if it is off, then <strong>New API Key</strong>: give it a name, an expiry, and either full access or a group.</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WIKIJS_URL</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">https://wiki.example.com</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WIKIJS_TOKEN</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">eyJhbGciOi…</span></span></code></pre></div><p><code>WIKIJS_API_KEY</code> is accepted as an alias, so moving here from another Wiki.js MCP server is a change of command, not of environment.</p><p>Wiki.js shows the key <strong>once</strong>. There is no way to read it back — <code>list_api_keys</code> returns only a truncated form — so a lost key has to be revoked and replaced.</p><h2 id="required-scopes" tabindex="-1">Required scopes <a class="header-anchor" href="#required-scopes" aria-label="Permalink to “Required scopes”">​</a></h2><p>Wiki.js scopes are per group and, for pages, per page rule. What this server needs depends on which tools you actually use:</p><table tabindex="0"><thead><tr><th>Doing this</th><th>Needs</th></tr></thead><tbody><tr><td>Listing and searching pages</td><td><code>read:pages</code></td></tr><tr><td>Reading page <strong>content</strong></td><td><code>read:source</code> — separate from <code>read:pages</code></td></tr><tr><td>History, versions, diffs</td><td><code>read:history</code></td></tr><tr><td>Creating and editing pages</td><td><code>write:pages</code></td></tr><tr><td>Moving and deleting pages</td><td><code>manage:pages</code>, <code>delete:pages</code></td></tr><tr><td>Comments</td><td><code>read:comments</code>, <code>write:comments</code>, <code>manage:comments</code></td></tr><tr><td>Assets</td><td><code>read:assets</code>, <code>write:assets</code>, <code>manage:assets</code></td></tr><tr><td>Users, groups, system, API keys</td><td><code>manage:system</code></td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title"><code>read:source</code> is the one that surprises people</p><p>It is separate from <code>read:pages</code>, and Wiki.js fails the <strong>whole</strong> query when a field-level scope is missing. This server therefore fetches metadata and content in two queries: without <code>read:source</code> you still get titles, paths and tags, plus a note saying why the text is missing, instead of &quot;page not found&quot;.</p></div><p>A key confined to a group also inherits that group&#39;s <strong>page rules</strong>, which is the only mechanism that hides pages from this server and the web UI alike. <code>WIKIJS_ALLOWED_PATHS</code> narrows writes on top of that, and <code>get_group</code> shows what a group is actually allowed.</p><h2 id="the-locale" tabindex="-1">The locale <a class="header-anchor" href="#the-locale" aria-label="Permalink to “The locale”">​</a></h2><p>Wiki.js treats the locale as part of a page&#39;s identity: <code>docs/setup</code> under <code>en</code> and under <code>de</code> are two different pages, not two renderings of one. Every tool here falls back to <code>WIKIJS_LOCALE</code> (default <code>en</code>), so on a wiki set up in another language:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WIKIJS_LOCALE</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">de</span></span></code></pre></div><p>Without it, every lookup by path answers &quot;not found&quot; on a wiki that plainly has the page. <code>list_locales</code> shows what is installed.</p><h2 id="confining-writes-to-part-of-the-wiki" tabindex="-1">Confining writes to part of the wiki <a class="header-anchor" href="#confining-writes-to-part-of-the-wiki" aria-label="Permalink to “Confining writes to part of the wiki”">​</a></h2><p><code>WIKIJS_ALLOWED_PATHS</code> limits the writing page and asset tools to a set of path prefixes:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WIKIJS_ALLOWED_PATHS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">docs,team/notes</span></span></code></pre></div><p>Matching is by path segment, so <code>docs</code> covers <code>docs/setup</code> and <strong>not</strong><code>docs-archive/old</code> — two page trees that merely begin with the same letters. <code>move_page</code> checks source and destination, so a page cannot be moved out of the allowed area or into a protected one.</p><p>Reads are deliberately <strong>not</strong> restricted. A scope on reads turns a wiki into a confusing half-wiki where the tree has holes, and the API key&#39;s own page rules are the right place to hide pages outright — they hide them from the browser too.</p><p>Three kinds of write it cannot confine, and refuses instead of pretending:</p><ul><li>The instance-wide maintenance tools act on every page there is: <code>purge_page_history</code>, <code>migrate_pages_locale</code>, <code>flush_page_cache</code>, <code>rebuild_page_tree</code> and <code>rebuild_search_index</code>. The last three lose nothing — they cost time — but &quot;every page in the wiki&quot; is outside any prefix all the same, so they are refused too.</li><li><code>update_tag</code> and <code>delete_tag</code> affect every page carrying the tag, wherever it lives.</li><li><code>update_comment</code> and <code>delete_comment</code> — Wiki.js does not report which page a comment belongs to, so there is no way to tell whether it is inside the allowed area. <code>create_comment</code> is given the page and is checked normally.</li></ul><p>Each of those refuses with a message naming the variable. Unset it to run them. <code>render_page</code> writes a single page and is checked against the prefix like any other page write — it changes no content, but it rewrites the stored HTML and bumps <code>updatedAt</code>, which is the field <code>update_page</code> reads to detect a concurrent edit.</p><p>Asset writes are checked against the <strong>asset folder</strong> path, which is a separate namespace from page paths: with <code>WIKIJS_ALLOWED_PATHS=docs</code>, uploads go into the asset folder <code>docs</code> or below, and the root is refused.</p><h2 id="tls" tabindex="-1">TLS <a class="header-anchor" href="#tls" aria-label="Permalink to “TLS”">​</a></h2><p>Use <code>https://</code>. Over plain http the API key travels unencrypted and the server prints a warning unless the host is loopback.</p><p>For an instance with a self-signed certificate, prefer adding your internal CA to the trust store. If that is genuinely not possible:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WIKIJS_INSECURE_TLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">true</span></span></code></pre></div><p>This is scoped to this server&#39;s own connections through a dedicated dispatcher — it does not set <code>NODE_TLS_REJECT_UNAUTHORIZED</code> and does not affect anything else in the process.</p><h2 id="read-only" tabindex="-1">Read-only <a class="header-anchor" href="#read-only" aria-label="Permalink to “Read-only”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WIKIJS_READ_ONLY</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">true</span></span></code></pre></div><p>The write tools are not registered at all: 27 tools instead of 62, and there is no call to refuse because there is no tool to call.</p><h2 id="turning-the-approval-dialog-off" tabindex="-1">Turning the approval dialog off <a class="header-anchor" href="#turning-the-approval-dialog-off" aria-label="Permalink to “Turning the approval dialog off”">​</a></h2><p>The destructive and administrative tools ask a person through MCP elicitation before they act. <code>ELICITATION=false</code> takes them to the two-call token instead. It does not remove the guard; there is no setting in which a guarded call goes unannounced.</p><p>The variable deliberately carries no <code>WIKIJS_</code> prefix, which means it reaches every MCP server in the same environment, and — unlike the booleans here — a value it does not recognise <strong>stops the server</strong> rather than failing off. See <a href="/guide/approval">Asking a person</a>.</p><h2 id="choosing-the-tools-that-load" tabindex="-1">Choosing the tools that load <a class="header-anchor" href="#choosing-the-tools-that-load" aria-label="Permalink to “Choosing the tools that load”">​</a></h2><p>Read-only mode is one cut, along a line this server drew for you. <code>WIKIJS_ALLOW_TOOLS</code> and <code>WIKIJS_DENY_TOOLS</code> let you draw your own:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WIKIJS_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">essential</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WIKIJS_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">get_page,search_pages,list_*</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WIKIJS_DENY_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">delete_*,purge_page_history,set_api_state</span></span></code></pre></div><p>Why bother, when all of them work: a model chooses the right tool far more reliably from a handful than from a long list, and every tool it can see costs context on every single request. If this is the only MCP server in a session, the full set is fine. If it is one of six, it is not.</p><p><strong>The syntax.</strong> Comma-separated entries. An entry is either an exact tool name or a prefix with a trailing <code>*</code> — <code>list_*</code> matches every tool whose name starts with <code>list_</code>. Entries are trimmed and case-insensitive, empty ones are ignored, and an empty value counts as unset. Nothing else is a pattern: <code>*_thing</code> and <code>list_*_x</code> are rejected rather than silently matching nothing.</p><p><strong><code>essential</code></strong> is a curated preset: <code>search_pages</code>, <code>grep_pages</code>, <code>get_page</code>, <code>list_pages</code>, <code>get_page_tree</code>, <code>create_page</code> and <code>update_page</code>. It is marked per tool in the <a href="/reference/tools">tool reference</a>, generated from the same constant the filter reads, so the two cannot drift. It composes — naming a tool alongside it puts that one back, and <code>WIKIJS_DENY_TOOLS</code> takes one away.</p><p><strong>Both together.</strong> <code>WIKIJS_ALLOW_TOOLS</code> decides what is in; <code>WIKIJS_DENY_TOOLS</code> is then subtracted from the result. With only a deny list, everything else stays.</p><p><strong>A name that matches nothing stops the server</strong>, with the offending entry and the list of real names. That is deliberate: the alternative is a tool quietly missing from <code>tools/list</code>, and nobody traces an absence back to an environment variable. The same applies to a pattern that matches no tool.</p><p><strong>With read-only mode</strong>, the write tools are not registered at all, so naming one explicitly in <code>WIKIJS_ALLOW_TOOLS</code> is an error that says so — rather than calling a tool unknown when it plainly exists. A <em>pattern</em> that covers write tools is fine and simply contributes nothing, which is what makes <code>get_*,create_*</code> a usable template for both kinds of deployment; and <code>WIKIJS_ALLOW_TOOLS=essential</code> narrows to the read half of the preset.</p><div class="tip custom-block"><p class="custom-block-title">It is the same cut, not a second one</p><p>A filtered tool is never registered, so it is absent from <code>tools/list</code> and unknown to <code>tools/call</code> alike — exactly what <code>WIKIJS_READ_ONLY</code> does to a write tool. There is no &quot;hidden but callable&quot; state to reason about.</p></div>`,46)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};