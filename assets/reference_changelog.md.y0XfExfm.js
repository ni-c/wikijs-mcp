import{H as e,Q as t,ht as n,nt as r,rt as i,tt as a,zt as o}from"./chunks/framework.Cs5aHXsR.js";var s=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1788117098000}`),c={name:`reference/changelog.md`};function l(e,s,c,l,u,d){return n(),a(`div`,null,[s[3]||=r(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="_0-2-0-2026-09-03" tabindex="-1">[0.2.0] - 2026-09-03 <a class="header-anchor" href="#_0-2-0-2026-09-03" aria-label="Permalink to “[0.2.0] - 2026-09-03”">​</a></h2><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li><p>Every tool declares an <code>outputSchema</code> and answers with <code>structuredContent</code> beside the text block. A client no longer has to parse prose to use a result — which twenty-nine of them made unavoidable, since they answered with a sentence. The sentence stays, in the text block.</p><p>The tools that report wiki content carry <code>untrusted: true</code> and <code>source: &quot;wikijs&quot;</code> as fields, not only as a preamble in the text. Page text, titles, descriptions and comments are written by anyone with edit rights, so a client that reads the structured half must not get them unframed. The list follows the call sites: a tool is marked exactly when it already routed its answer through the untrusted wrapper.</p><p>Wiki.js records are described as open objects with the top-level keys this server builds. A self-hosted Wiki.js is any 2.x release, and a strict shape would turn a field one adds into a tool that fails outright.</p></li><li><p>Tools that need a confirmation now <strong>ask the user</strong>, on clients that can show a prompt. The two-call <code>confirm_token</code> remains for clients that cannot, so nothing that works today stops working — but where a person can be asked, one is, instead of a token that only proves the same call was made twice.</p></li><li><p><code>ELICITATION</code> switches the dialog off — <code>false</code> sends a client that could have been asked down the two-call-token path instead. For a scheduled job or a test harness, where a dialog is the wrong shape rather than an unwanted one.</p><p>It does <strong>not</strong> remove the guard: there is no setting in which a guarded call goes unannounced. Two deliberate rough edges come with it. The variable is <strong>not prefixed</strong>, so one <code>export ELICITATION=false</code> reaches every MCP server in the environment — which is why a server started with it off prints a line saying so, and why the fallback text names the server instead of blaming a client that was working fine. And a value that is neither <code>true</code> nor <code>false</code><strong>stops the server</strong>, where the <code>WIKIJS_*</code> booleans beside it fail <em>off</em> on a typo: this is the only variable here that defaults to <em>on</em>. It is read after <code>WIKIJS_TOKEN</code> and <code>WIKIJS_API_KEY</code> are wiped from the environment, so that exit cannot leave a credential behind.</p></li><li><p>A <code>docs/guide/approval.md</code> page, and a 👤 marker in the generated tool reference that is read off the registered schema rather than from a list kept beside it.</p></li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li><p>The advertised schemas avoid a spelling that is legal JSON Schema and still gets a tool refused, or its constraint silently dropped, by some MCP clients: an open object now writes <code>&quot;additionalProperties&quot;: true</code> rather than the empty schema <code>{}</code> zod emits for it. What the tools accept and return is unchanged; only the way the schema says so is.</p></li><li><p><code>update_page</code>&#39;s stale-read refusal answers <code>{page_id, written: false, conflict: {you_saw, it_is_now}, note}</code> in addition to its sentence. It is still <strong>not</strong> an error result — an <code>isError</code> would make a client surface it as a failure, when what it is is an instruction for recovering.</p></li><li><p>A result too large to shrink is now an error rather than an envelope saying so. The envelope was a different shape from what the tool declares it returns, which the SDK refuses.</p></li><li><p>The two-call <code>confirm_token</code> prompt is an error result. What was asked for did not happen, which is what <code>isError</code> says. The text is unchanged and still carries the token.</p></li><li><p>The integration compose file publishes Wiki.js on <code>WIKIJS_PORT</code> (default 3010) instead of a hardcoded 3010, so a workstation that already runs something there does not need a patched compose file.</p></li><li><p>Runs on <strong>MCP SDK 2.0</strong>. Existing clients see the same protocol revision they always did; the change is the package layout behind it, and it is what lets the dialog above work on both protocol eras from one code path — including behind a stateless gateway, where the older mechanism silently fell back to the weaker token for every client.</p></li><li><p>The linter is <strong>oxlint</strong> instead of eslint plus typescript-eslint, which lifts the TypeScript ceiling: typescript-eslint pins <code>typescript</code> below 6.1, so this repository was held on TypeScript 6 by its linter rather than by its code.</p></li><li><p>The tool filter, the confirmation store and the documentation-asset generator now come from <strong><code>mcp-tool-allowlist</code></strong>, <strong><code>mcp-approval</code></strong> and <strong><code>svg-asset-set</code></strong> rather than from copies kept here — 571 fewer lines, and one place to fix each. None of them has a runtime dependency of its own.</p></li><li><p><code>WIKIJS_READ_ONLY</code> now accepts <code>1</code> and <code>yes</code> as well as <code>true</code>, trimmed and case-insensitively. <code>WIKIJS_INSECURE_TLS</code> deliberately still takes only the exact word: a switch that <strong>adds</strong> a protection should honour anything an operator plausibly meant by &quot;on&quot;, and one that <strong>removes</strong> a protection should not.</p></li><li><p>stdio is served through <code>serveStdio</code>, so the connection&#39;s era is negotiated on the opening exchange rather than assumed. A client that pins the <code>2026-07-28</code> era is served it; until now its <code>server/discover</code> probe was answered with &quot;Method not found&quot; and only <code>2025-11-25</code> was on offer. A client that speaks the older era sees no change — it is still pinned to one instance for the life of the connection, exactly as a hand-wired <code>StdioServerTransport</code> served it.</p></li></ul><h3 id="removed" tabindex="-1">Removed <a class="header-anchor" href="#removed" aria-label="Permalink to “Removed”">​</a></h3><ul><li><p><strong><code>flush_page_cache</code>, <code>rebuild_page_tree</code> and <code>rebuild_search_index</code> no longer ask for a confirmation</strong>, and no longer declare <code>confirm_token</code> at all — a caller that still sends one gets a schema error rather than silence.</p><p>They were gated because they are instance-wide and slow, and that is the wrong argument: nothing is lost by any of them. The cost is time, not content, and a dialog in front of an operation that loses nothing is how people learn to tick without reading — which spends exactly the attention <code>purge_page_history</code> needs. What each of them does cost is now stated in its description.</p></li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p><strong>A confirmation for <code>update_group</code> did not cover the group&#39;s name or its login redirect</strong>, although the mutation replaces both. A token issued for a change to the page rules would execute a call that also renamed the group — to &quot;Administrators&quot;, say, which is what every administration view and every later <code>list_groups</code> answer then reports — and repointed <code>redirectOnLogin</code>, where Wiki.js sends a member once it has authenticated them. Both are now bound, and named in the prompt. <code>create_user</code> had the same gap for <code>password</code>, <code>provider_key</code>, <code>must_change_password</code> and <code>send_welcome_email</code>.</p><p>Found by reading; kept out by <code>test/confirmation-binding.test.ts</code>, which varies every argument of every gated tool in turn and requires the token to break — and which fails when a gated tool gains an argument nobody accounted for.</p></li><li><p><strong>A refusal written by Wiki.js itself went into the model&#39;s context unbounded and unmarked.</strong> <code>responseResult.message</code> is as far upstream as a database driver — a comment with a null <code>replyTo</code> comes back with the whole INSERT statement in it — and that one error path skipped the 2000-character cap the other three go through. It is capped now, and set off under a line saying the upstream wrote it, rather than folded into this server&#39;s own sentence. The error slug is reduced to the shape a slug has, so it cannot open a line of its own beside that marker.</p></li><li><p><strong><code>grep_pages</code> applied <code>path_prefix</code> after the 200-page ceiling instead of before it.</strong> On a wiki whose most recently updated pages are all under <code>blog/</code>, a search under <code>docs/</code> answered &quot;0 pages scanned, 0 matched&quot; without reading a single one of the pages that did match. The ceiling exists to bound the fetch, so it now applies to the pages that survive the filter; the accompanying note counts those too, since that is the number a caller can act on.</p></li><li><p><strong><code>identifier()</code> let the bidi control characters through</strong>, including RIGHT-TO-LEFT OVERRIDE. Its rule listed four invisible characters and the class has rather more, so <code>move_page</code> would show a destination path that rendered backwards from the one it was about to write. The rule is now stated as the Unicode categories it always meant (<code>Cc</code>, <code>Cf</code>, <code>Zl</code>, <code>Zp</code>, <code>Zs</code>) rather than as a list of code points.</p></li><li><p><strong><code>WIKIJS_ALLOWED_PATHS</code> did not cover four of the tools the documentation said it covered.</strong> <code>flush_page_cache</code>, <code>rebuild_page_tree</code> and <code>rebuild_search_index</code> act on every page in the wiki and now refuse while the variable is set, like the two destructive maintenance tools already did — they lose nothing, but &quot;the whole wiki&quot; is outside any prefix. And <code>render_page</code> is now placed against the prefix like every other page write: it changes no content, but it rewrites the stored HTML and bumps <code>updatedAt</code>, which is the field <code>update_page</code> reads to detect somebody else&#39;s edit.</p></li><li><p>An entry in <code>WIKIJS_ALLOW_TOOLS</code> that is not tool-name-shaped is now <strong>redacted</strong> in the error rather than quoted back. <code>WIKIJS_API_KEY</code> and <code>WIKIJS_ALLOW_TOOLS</code> are adjacent lines in every compose file, and a paste into the wrong one used to print the credential into the client&#39;s log.</p></li></ul><h2 id="_0-1-2-2026-08-30" tabindex="-1">[0.1.2] - 2026-08-30 <a class="header-anchor" href="#_0-1-2-2026-08-30" aria-label="Permalink to “[0.1.2] - 2026-08-30”">​</a></h2><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to “Fixed”">​</a></h3>`,12),t(`ul`,null,[s[2]||=t(`li`,null,[t(`p`,null,[t(`strong`,null,`The container and documentation badges rendered as broken images.`),i(` Both were mis-escaped: shields.io reads a literal hyphen as a field separator, so `),t(`code`,null,`ghcr.io-ni--c%2Fwikijs-mcp-blue`),i(` splits in the wrong place and the endpoint answers 404. The `),t(`code`,null,`ni--c`),i(` half was escaped correctly in both, which is what made it easy to miss. This release is what carries the fix to the package page — a README only reaches npmjs.com with a publish.`)])],-1),t(`li`,null,[t(`p`,null,[s[0]||=t(`strong`,null,`The issue forms shipped with their template placeholders unresolved.`,-1),s[1]||=i(` All three contact links pointed at `,-1),t(`code`,null,`github.com/ni-c/`+o(e.REPO)+`/…`,1),i(`, so both "Question or discussion" and "Report a vulnerability privately" led to a 404 — the second being the one that matters, since the alternative a reporter reaches for is a public issue. The forms also asked for a "`+o(e.REPO)+` version" and a "`+o(e.TARGET_SYSTEM)+` API endpoint"; they now name wikijs-mcp, Wiki.js and its GraphQL schema.`,1)])])]),s[4]||=r(`<h2 id="_0-1-1-2026-08-30" tabindex="-1">[0.1.1] - 2026-08-30 <a class="header-anchor" href="#_0-1-1-2026-08-30" aria-label="Permalink to “[0.1.1] - 2026-08-30”">​</a></h2><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><code>list_pages</code> and <code>grep_pages</code> returned far fewer pages than asked for, with nothing saying so. Wiki.js&#39; <code>pages.list</code> joins the tag table and applies <code>limit</code> to the <strong>joined rows</strong>, not to the pages — a page with three tags eats three of them — so asking for 50 on a 62-page wiki returned between 13 and 23 depending on the sort order. A short answer was indistinguishable from a small wiki. Both tools now bound the list themselves, and <code>list_pages</code> reports how many pages matched as well as how many are shown.</li><li>The demo recording is now actually shown in the README and on the documentation home page. It was recorded and checked in, and nothing referenced it.</li></ul><h2 id="_0-1-0-2026-08-30" tabindex="-1">[0.1.0] - 2026-08-30 <a class="header-anchor" href="#_0-1-0-2026-08-30" aria-label="Permalink to “[0.1.0] - 2026-08-30”">​</a></h2><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><ul><li>Initial release: MCP server for Wiki.js 2.x, with 62 tools covering pages, history, tags, assets, comments, users, groups, system settings and maintenance.</li><li><code>grep_pages</code>, which searches the actual text of pages. Wiki.js&#39; default search engine (&quot;Database - Basic&quot;) indexes only titles and descriptions, so <code>search_pages</code> cannot find anything written <em>inside</em> a page — it now reports which engine is active and says so.</li><li>A concurrent-edit guard on <code>update_page</code>. It compares against the moment the caller read the page, refuses to overwrite somebody else&#39;s save, and can be overridden deliberately with <code>force</code>.</li><li>Surgical edits: <code>update_page</code> takes <code>edits</code> with a find-and-replace contract that requires each anchor to match exactly once, rather than silently editing the first occurrence.</li><li><code>get_page</code> modes for metadata, an outline, a named section, and a character window, so a large page can be read without filling the context.</li><li><code>diff_page_versions</code>, a unified diff between two stored versions or between a version and the live page.</li><li><code>upload_asset</code>, which Wiki.js 2.x has no GraphQL mutation for.</li><li><code>WIKIJS_ALLOWED_PATHS</code>, confining the writing tools to page path prefixes, matched by path segment.</li><li><code>WIKIJS_READ_ONLY</code>, <code>WIKIJS_ALLOW_TOOLS</code> / <code>WIKIJS_DENY_TOOLS</code> with an <code>essential</code> preset, and confirmation tokens on every destructive or administrative operation.</li></ul>`,6)])}var u=e(c,[[`render`,l]]);export{s as __pageData,u as default};